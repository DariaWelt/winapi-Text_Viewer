#include "header.h"

//int GetSizeOfFile(char* fileName) {
//    FILE* file = fopen(fileName, "r");
//    if(file == NULL) {
//        return -1;
//    }
//    fseek(file, 0, SEEK_END);
//    int size = ftell(file);
//    fclose(file);
//    return size;
//}


void __init__(textData_t* model, viewerText_t* viewer) {
    model->
}
